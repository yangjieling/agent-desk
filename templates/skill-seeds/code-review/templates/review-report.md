# Code Review Report

> 落盘文件名：`{output_dir}/review-{YYYYMMDD-HHmm}.md`
> 需求审查模式与中间产物 `requirements-{YYYYMMDD-HHmm}.md` 共用同一时间戳，方便配对
>
> **模式裁剪规则**：需求审查保留完整模板；基础审查删除摘要中的「需求来源」「需求统计」「反向扫描」三行，以及「一、需求覆盖矩阵」「二、偏离与遗漏」「三、超范围改动」三个章节。其余章节保留，但删除其中仅用于 PRD 归因或需求 ID 关联的字段与示例。

---

## 摘要

| 项 | 值 |
|----|----|
| 需求来源 | `<PRD 解构 (path) | 用户清单 (path)（原始 PRD: path 或 缺失）>` |
| 变更范围 | `<base>...HEAD`（`<N>` files） |
| 文件统计 | 新增 `<n>` / 修改 `<n>` / 删除 `<n>` / 二进制 `<n>` |
| 需求统计 | ✅ 已实现 `<n>`  🟡 部分 `<n>`  ❌ 未实现 `<n>`  ⚠️ 偏离 `<n>` |
| 反向扫描 | ➕ 超范围 `<n>`  ⚡ 实现风险 `<n>` |
| 规范违规 | 📐 P0 `<n>`  P1 `<n>`  P2 `<n>`  （豁免 `<n>`） |
| 数据库 | 🗄️ 表变更 `<n>`  ⚠️ 物料缺失 `<n>`  📐 D系列 P0:`<n>` P1:`<n>` P2:`<n>`  🟡 疑似 `<n>`  ➕ XDB `<n>` |
| 代码硬伤 | 🔴 高 `<n>`  🟡 中 `<n>` |
| 风险等级 | `<🔴 高 | 🟡 中 | 🟢 低>` |
| 生成时间 | `<YYYY-MM-DD HH:mm>` |

---

## 一、需求覆盖矩阵

| ID | 需求点 | 状态 | 关联代码 | 备注 |
|----|--------|:----:|----------|------|
| R001 | ... | ✅ | `path/to/File.java:123` | — |
| R002 | ... | 🟡 | `...:45` | 缺失锁定时长配置项 |
| R003 | ... | ❌ | — | PRD 提及但 diff 未涉及 |
| R004 | ... | ⚠️ | `...:67` | 阈值与 PRD 不符（5 vs 3） |

---

## 二、偏离与遗漏（重点）

> 本节聚焦 ❌ 未实现 / ⚠️ 偏离 / 🟡 部分实现 三类，逐条详述

### 2.1 [R003] 锁定时长配置化未实现 ❌

- **PRD 原文**：`<引文片段>`
- **实际实现**：代码中锁定时长写死 `1800L`，未读取配置
- **影响**：运营无法热调整封禁策略，违反 PRD「可配置」诉求
- **建议**：抽取为 `auth.lock.duration-seconds` 配置项
- **位置**：`com/example/auth/LoginService.java:123`

**是否采用此建议**：○ 是  ○ 否  ○ 部分采用

```java
// 当前实现
if (failCount >= 5) {
    redis.expire(key, 1800L);  // ❌ 硬编码
}
```

（其余条目按相同结构）

---

## 三、超范围改动（反向扫描）

| ID | 位置 | 改动概述 | 关联需求 | 处置建议 | 是否采用 |
|----|------|---------|----------|---------|:--------:|
| X001 | `path/file:line` | <一句话> | 无 | 确认是否需补到 PRD / 单独 PR | ○ 是 ○ 否 ○ 部分采用 |
| XDB001 | `mapper/OrderLogMapper.xml:45` | PRD 未提及的数据库 INSERT | 无 | 确认是否补充 PRD 与表结构，或拆分为单独变更 | ○ 是 ○ 否 ○ 部分采用 |

---

## 四、实现风险（隐含项，影响已声明需求）

> 不处理则**已声明需求**无法正确工作。已扣除 §五 数据库规范与 §七 团队规范命中项

### 4.1 [⚡ 关联 R001] 手机号 trim 前未做 null 校验

- **位置**：`com/example/auth/LoginController.java:45`
- **风险**：当前端漏传 `phone` 时直接 NPE，导致 R001 主路径 500
- **建议**：使用 `StringUtils.trimToEmpty(phone)` 或前置 null 检查

**是否采用此建议**：○ 是  ○ 否  ○ 部分采用

```java
String phone = req.getPhone().trim();  // ⚠️ NPE 风险
```

---

## 五、数据库变更评估

> 数据库相关需求必须在 PRD 中提供可审查的表结构。若 PRD 缺失表结构，即使代码库中能找到 DDL，也要在本节标注物料缺失。

### 5.1 检测到的数据库操作清单

| 表名 | 操作类型 | 文件位置 | 关联 ID |
|------|----------|----------|---------|
| `user_score` | ALTER ADD COLUMN | `sql/V1.2__add_score.sql` | R003 |
| `user_score` | SELECT | `UserScoreMapper.xml:23` | R003 |
| `order_log` | INSERT | `OrderLogMapper.xml:45` | XDB001（超范围） |

### 5.2 物料完整性核对

判定标准见 [`templates/requirement-list.md`](requirement-list.md) 的「数据库物料完整性判定」。

| 表名 | PRD 表结构来源 | 审查 schema 来源 | 完整性 | 关联 ID | 说明 |
|------|----------------|------------------|:------:|---------|------|
| `user_score` | PRD §3.2 已附完整 DDL | PRD §3.2 + `sql/schema.sql` | ✅ | R003 | — |
| `order_log` | 缺失 | `sql/schema.sql` | ❌ | XDB001 | PRD 未提及该表；代码库有 DDL，但 PRD 物料仍缺失 |
| `read_only_view` | PRD §4.1 仅业务描述 | `sql/schema.sql` | 🟡 | R005 | 缺索引/唯一键摘要 |

物料缺失处理：

- 不改写需求四态；仅在需求矩阵备注中标注 `⚠️ 数据库物料缺失，需补充后复审`
- 缺失项列入行动建议；涉及 DDL / INSERT / UPDATE / DELETE / 唯一性约束 / 状态流转字段时按高风险处理

### 5.3 数据库规范扫描结果

**违规清单**（D 系列，参与多维去重）：

| 规则 ID | 严重级 | 文件:行 | 关联 ID | 命中描述 | 是否采用建议 |
|---------|--------|---------|---------|----------|:------------:|
| D201 | P0 | `OrderLogMapper.xml:12` | XDB001 | UPDATE 无 WHERE | ○ 是 ○ 否 ○ 部分采用 |
| D202 | P1 | `UserScoreMapper.xml:23` | R003 | 使用 SELECT * | ○ 是 ○ 否 ○ 部分采用 |

**疑似违规清单**（缺少 schema / 索引 / 运行时信息 / 业务数据分布 / 上游初始化链路 / SDK 语义等闭合证据；提示潜在影响，但不按确认 P0 处理）：

| 规则 ID | 严重级 | 文件:行 | 关联 ID | 疑似原因 / 待确认信息 | 是否采用建议 |
|---------|--------|---------|---------|----------------------|:------------:|
| D109 | 疑似 P0 | `UserScoreMapper.xml:23` | R003 | 待确认唯一键是否为 `(shop_id,user_id,biz_date)`，以及业务上是否会命中多行；当前 WHERE 漏 `user_id`，若多行存在可能扩大更新范围 | ○ 是 ○ 否 ○ 部分采用 |

**候选关联（待确认）**：

| 位置 | 候选需求 | 证据 | 优先级 | 处置建议 | 是否采用 |
|------|----------|------|--------|----------|:--------:|
| `ScoreMapper.xml:18` | R003 | Mapper 命名与需求实体匹配 | P1 | 人工确认是否归属 R003；确认后补齐 `关联表` | ○ 是 ○ 否 ○ 部分采用 |

**数据库豁免清单**：

| 位置 | 规则 ID | 豁免理由 |
|------|---------|----------|
| `sql/V1.2__migrate.sql:8` | D201 | `<行内 review-skip 注释中的 reason>` |

---

## 六、关键代码质量问题

> 与具体需求无关、影响功能正确性的硬伤。已扣除 §四 / §五 / §七 命中项

### 6.1 [🔴 高] SQL 拼接

- **位置**：`...:88`
- **类型**：SQL 注入
- **片段**：
- **建议**：改用 PreparedStatement / MyBatis 参数绑定

**是否采用此建议**：○ 是  ○ 否  ○ 部分采用

---

## 七、团队规范合规性

> 按 P0 / P1 / P2 分组；规则定义见 [`references/team-style-guide.md`](../references/team-style-guide.md)

### 7.1 P0（必须修复）

#### [S001] Controller 应使用构造器注入 — `path/file:line`

```java
@Autowired
private UserService userService;  // ❌
```

**理由**：（引用规则原文）
**修复**：改为构造器注入或 `@RequiredArgsConstructor`

**是否采用此建议**：○ 是  ○ 否  ○ 部分采用

### 7.2 P1（强烈建议）

（同上结构）

### 7.3 P2（建议）

（同上结构）

### 7.4 主动豁免汇总

| 位置 | 规则 ID | 豁免理由 |
|------|---------|----------|
| `path:line` | S00X | `<行内 review-skip 注释中的 reason>` |

---

## 八、行动建议

按 P0 → P1 → P2 排序：

### 8.1 P0（必须修复）

| 序号 | 建议内容 | 是否采用 |
|------|---------|:--------:|
| 1 | `[R003]` 实现锁定时长配置化 | ○ 是 ○ 否 ○ 部分采用 |
| 2 | `[数据库物料] R003` 补充 `user_score` 表结构到 PRD（字段、类型、NULL/默认值、主键、唯一键、索引、字段说明） | ○ 是 ○ 否 ○ 部分采用 |
| 3 | `[S001] LoginController.java:23` 改用构造器注入 | ○ 是 ○ 否 ○ 部分采用 |
| 4 | `[🔴] UserController.java:88` 修复 SQL 注入 | ○ 是 ○ 否 ○ 部分采用 |

### 8.2 P1（强烈建议）

| 序号 | 建议内容 | 是否采用 |
|------|---------|:--------:|
| 1 | ... | ○ 是 ○ 否 ○ 部分采用 |

### 8.3 P2（建议）

| 序号 | 建议内容 | 是否采用 |
|------|---------|:--------:|
| 1 | ... | ○ 是 ○ 否 ○ 部分采用 |

---

## 附录：未审查文件

| 文件 | 原因 |
|------|------|
| `path/to/binary` | 二进制 |
| `path/to/large.json` | 自动生成 |
